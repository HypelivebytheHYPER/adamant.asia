# pnpm settings
package-manager=pnpm
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true

# pnpm does not read overrides from .npmrc; use pnpm.overrides in package.json instead
